// Location tracker hook - disabled (customer_locations table does not exist)
// Kept as a no-op stub so existing imports don't break.

export const useLocationTracker = () => {
  // No-op: geolocation tracking is not currently active
};
